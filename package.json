{
  "name": "my-api",
  "version": "1.0.0",
  "description": "Simple RESTful API with Node.js and Express",
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --forceExit",
    "clean": "rm -rf dist",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "express": "^4.18.2",
    "jest": "^27.5.1",
    "nodemon": "^3.0.3",
    "supertest": "^6.3.4"
  }
}
